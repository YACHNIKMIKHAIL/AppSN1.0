import React from 'react';

const Login = () => {
    return (
        <div style={{color:'yellow',fontSize:'50px'}}>
            LOGIN
            LOGIN
            LOGIN
            LOGIN
            LOGIN
        </div>
    );
};

export default Login;